# RecallBase Perf Checks

Perf tests are regression signals for high-frequency commands. V1 targets "seconds, not minutes" for repeated imports and local read commands on fixture corpora.

Browser extension performance gates now live in the sibling `RecallBase-Extension` project.
