Sanitized Codex JSONL fixture.

Shape covered:
- Dated JSONL session files under a Codex-like directory.
- Top-level `timestamp`, `type`, and `payload`.
- User, assistant, tool, unknown, duplicate, and malformed JSONL records.

No real prompts, paths, repository names, tokens, or account identifiers are included.
