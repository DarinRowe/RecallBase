Sanitized GitHub Copilot chatSessions fixture.

Shape covered:
- VS Code workspace storage with `chatSessions/*.json`.
- `sessionId`, `creationDate`, `lastMessageDate`, `requests`, user messages, assistant response chunks, and workspace metadata.

No real prompts, paths, repository names, tokens, or account identifiers are included.
