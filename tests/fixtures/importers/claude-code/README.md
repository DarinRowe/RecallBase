Sanitized Claude Code JSONL fixture.

Shape covered:
- Project/session JSONL with `sessionId`, `type`, `permissionMode`, timestamps, and message objects.
- User, assistant, tool result, summary, and malformed JSONL records.

No real prompts, paths, repository names, tokens, or account identifiers are included.
